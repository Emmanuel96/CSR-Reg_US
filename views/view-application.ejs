<!DOCTYPE html>
<html>
<head>
  <title>View Application</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 20px; }
    h1 { color: #333; }
    .application { margin-bottom: 20px; border: 1px solid #ccc; padding: 10px; }
    .label { font-weight: bold; }
  </style>
</head>
<body>
  <% if (error) { %>
    <h1>Error</h1>
    <p><%= error %></p>
    <a href="/">Return to Home</a>
  <% } else { %>
    <h1>Application: <%= application.organisation_name || 'Unnamed' %></h1>
    <div class="application">
      <p><span class="label">Contact Person:</span> <%= application.contact_person || 'Not provided' %></p>
      <p><span class="label">Organization Name:</span> <%= application.organisation_name || 'Not provided' %></p>
      <p><span class="label">Address:</span> <%= application.organisation_address || 'Not provided' %></p>
      <p><span class="label">Country:</span> <%= application.country || 'Not provided' %></p>
      <p><span class="label">Telephone:</span> <%= application.telephone_number || 'Not provided' %></p>
      <p><span class="label">Nationality:</span> <%= application.organisation_nationality || 'Not provided' %></p>
      <p><span class="label">Postal Code:</span> <%= application.postal_code || 'Not provided' %></p>

      <p><span class="label">Email:</span> <%= application.email_address || 'Not provided' %></p>
      <p><span class="label">Mobile:</span> <%= application.mobile_number || 'Not provided' %></p>
      <p><span class="label">Sole Trader/Micro:</span> <%= application.soleTraderMicro || 'Not provided' %></p>
      <p><span class="label">Charity:</span> <%= application.charity || 'Not provided' %></p>
      <h3>Introduction</h3>
      <p><%= application.introduction || 'Not provided' %></p>
      <h3>Environment</h3>
      <p><span class="label">Energy:</span> <%= application.env_energy || 'Not provided' %></p>
      <p><span class="label">Natural Resource:</span> <%= application.env_natural_resource || 'Not provided' %></p>
      <p><span class="label">Travel:</span> <%= application.env_travel || 'Not provided' %></p>
      <p><span class="label">Supply Chain:</span> <%= application.env_supply_chain_management || 'Not provided' %></p>
      <p><span class="label">Waste:</span> <%= application.env_waste || 'Not provided' %></p>
      <h3>Workplace</h3>
      <p><%= application.workplace || 'Not provided' %></p>
      <h3>Community</h3>
      <p><%= application.community || 'Not provided' %></p>
      <h3>Philanthropy</h3>
      <p><%= application.philanthropy || 'Not provided' %></p>
      <h3>Other Information:</h3>
      <p><%= application.phil_other_information || 'Not provided' %></p>
      <h3>Future Planning:</h3>
      <p><%= application.phil_future_planning || 'Not provided' %></p>
    </div>
    <p><em>This is a view-only page. You cannot edit this application.</em></p>
  <% } %>
</body>
</html>